---
interface Props {
  variant?: 'white' | 'soft';
  elevation?: 'none' | 'sm' | 'lg';
  padding?: 'none' | 'sm' | 'md' | 'lg' | 'xl';
  class?: string;
}

const { 
  variant = 'white', 
  elevation = 'sm', 
  padding = 'md',
  class: className 
} = Astro.props;

const variants = {
  white: 'bg-canvas text-ink',
  soft: 'bg-canvas-soft text-ink',
};

const elevations = {
  none: '',
  sm: 'shadow-[0_2px_8px_rgba(0,0,0,0.03),0_1px_3px_rgba(0,0,0,0.02),inset_0_1px_0_rgba(255,255,255,0.7)] border border-hairline/60',
  lg: 'shadow-2xl border border-hairline/80 ring-1 ring-hairline/50',
};

const paddings = {
  none: '',
  sm: 'p-3',
  md: 'p-6',
  lg: 'p-8',
  xl: 'p-10',
};
---

<div class:list={[
  'rounded-md transition-all',
  variants[variant],
  elevations[elevation],
  paddings[padding],
  className
]}>
  <slot />
</div>
